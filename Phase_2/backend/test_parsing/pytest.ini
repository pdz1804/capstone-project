[pytest]
markers =
    mock: Mock tests (no real files or API calls, fast)
    full: Full tests with real Excel files from disk
    e2e: End-to-end tests with real LLM calls (costs money)
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
