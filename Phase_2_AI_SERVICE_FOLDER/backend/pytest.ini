[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    unit: no external services required (mocked)
    integration: optional real Qdrant / network
    gpu: requires CUDA for local ColQwen (skipped if unavailable)
